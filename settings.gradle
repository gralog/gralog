// Avoid cryptic errors by checking the Java version early.  Java 8 is
// also the first JDK to include JavaFX, which we also need.
if(!JavaVersion.current().isJava8Compatible())
  throw new GradleException(
    'ERROR: To compile gralog, please install the Java Development Kit (JDK) 8 or later.')

include 'dockfx'
include 'gralog-automaton'
include 'gralog-computation-tree-logic'
include 'gralog-core'
include 'gralog-finite-game'
include 'gralog-first-order-logic'
include 'gralog-fx'
include 'gralog-modal-logic'
include 'gralog-modal-mu-calculus'
include 'gralog-np-completeness'
include 'gralog-jgrapht-bridge'
